package basic7;
import battlecode.common.*;

public class ResourcePatternManager {


    static MapLocation l2;
    static MapInfo m2;
    static MapLocation l3;
    static MapInfo m3;
    static MapLocation l4;
    static MapInfo m4;
    static MapLocation l5;
    static MapInfo m5;
    static MapLocation l6;
    static MapInfo m6;
    static MapLocation l10;
    static MapInfo m10;
    static MapLocation l11;
    static MapInfo m11;
    static MapLocation l12;
    static MapInfo m12;
    static MapLocation l13;
    static MapInfo m13;
    static MapLocation l14;
    static MapInfo m14;
    static MapLocation l15;
    static MapInfo m15;
    static MapLocation l16;
    static MapInfo m16;
    static MapLocation l18;
    static MapInfo m18;
    static MapLocation l19;
    static MapInfo m19;
    static MapLocation l20;
    static MapInfo m20;
    static MapLocation l21;
    static MapInfo m21;
    static MapLocation l22;
    static MapInfo m22;
    static MapLocation l23;
    static MapInfo m23;
    static MapLocation l24;
    static MapInfo m24;
    static MapLocation l25;
    static MapInfo m25;
    static MapLocation l26;
    static MapInfo m26;
    static MapLocation l27;
    static MapInfo m27;
    static MapLocation l28;
    static MapInfo m28;
    static MapLocation l29;
    static MapInfo m29;
    static MapLocation l30;
    static MapInfo m30;
    static MapLocation l31;
    static MapInfo m31;
    static MapLocation l32;
    static MapInfo m32;
    static MapLocation l33;
    static MapInfo m33;
    static MapLocation l34;
    static MapInfo m34;
    static MapLocation l35;
    static MapInfo m35;
    static MapLocation l36;
    static MapInfo m36;
    static MapLocation l37;
    static MapInfo m37;
    static MapLocation l38;
    static MapInfo m38;
    static MapLocation l39;
    static MapInfo m39;
    static MapLocation l40;
    static MapInfo m40;
    static MapLocation l41;
    static MapInfo m41;
    static MapLocation l42;
    static MapInfo m42;
    static MapLocation l43;
    static MapInfo m43;
    static MapLocation l44;
    static MapInfo m44;
    static MapLocation l45;
    static MapInfo m45;
    static MapLocation l46;
    static MapInfo m46;
    static MapLocation l47;
    static MapInfo m47;
    static MapLocation l48;
    static MapInfo m48;
    static MapLocation l49;
    static MapInfo m49;
    static MapLocation l50;
    static MapInfo m50;
    static MapLocation l51;
    static MapInfo m51;
    static MapLocation l52;
    static MapInfo m52;
    static MapLocation l53;
    static MapInfo m53;
    static MapLocation l54;
    static MapInfo m54;
    static MapLocation l55;
    static MapInfo m55;
    static MapLocation l56;
    static MapInfo m56;
    static MapLocation l57;
    static MapInfo m57;
    static MapLocation l58;
    static MapInfo m58;
    static MapLocation l59;
    static MapInfo m59;
    static MapLocation l60;
    static MapInfo m60;
    static MapLocation l61;
    static MapInfo m61;
    static MapLocation l62;
    static MapInfo m62;
    static MapLocation l64;
    static MapInfo m64;
    static MapLocation l65;
    static MapInfo m65;
    static MapLocation l66;
    static MapInfo m66;
    static MapLocation l67;
    static MapInfo m67;
    static MapLocation l68;
    static MapInfo m68;
    static MapLocation l69;
    static MapInfo m69;
    static MapLocation l70;
    static MapInfo m70;
    static MapLocation l74;
    static MapInfo m74;
    static MapLocation l75;
    static MapInfo m75;
    static MapLocation l76;
    static MapInfo m76;
    static MapLocation l77;
    static MapInfo m77;
    static MapLocation l78;
    static MapInfo m78;

    static RobotController rc;

    static boolean ready;
    static boolean maxT;
    static MapLocation getBestTarget () throws GameActionException {

        rc = MyRobot.rc;
        ready = rc.isActionReady() && rc.getPaint() > 10;
        maxT = Util.towerMax();

        MapLocation myLoc = rc.getLocation();
        l2 = myLoc.translate(-4,-2);
        if (rc.canSenseLocation(l2)) m2 = rc.senseMapInfo(l2);
        l3 = myLoc.translate(-4,-1);
        if (rc.canSenseLocation(l3)) m3 = rc.senseMapInfo(l3);
        l4 = myLoc.translate(-4,0);
        if (rc.canSenseLocation(l4)) m4 = rc.senseMapInfo(l4);
        l5 = myLoc.translate(-4,1);
        if (rc.canSenseLocation(l5)) m5 = rc.senseMapInfo(l5);
        l6 = myLoc.translate(-4,2);
        if (rc.canSenseLocation(l6)) m6 = rc.senseMapInfo(l6);
        l10 = myLoc.translate(-3,-3);
        if (rc.canSenseLocation(l10)) m10 = rc.senseMapInfo(l10);
        l11 = myLoc.translate(-3,-2);
        if (rc.canSenseLocation(l11)) m11 = rc.senseMapInfo(l11);
        l12 = myLoc.translate(-3,-1);
        if (rc.canSenseLocation(l12)) m12 = rc.senseMapInfo(l12);
        l13 = myLoc.translate(-3,0);
        if (rc.canSenseLocation(l13)) m13 = rc.senseMapInfo(l13);
        l14 = myLoc.translate(-3,1);
        if (rc.canSenseLocation(l14)) m14 = rc.senseMapInfo(l14);
        l15 = myLoc.translate(-3,2);
        if (rc.canSenseLocation(l15)) m15 = rc.senseMapInfo(l15);
        l16 = myLoc.translate(-3,3);
        if (rc.canSenseLocation(l16)) m16 = rc.senseMapInfo(l16);
        l18 = myLoc.translate(-2,-4);
        if (rc.canSenseLocation(l18)) m18 = rc.senseMapInfo(l18);
        l19 = myLoc.translate(-2,-3);
        if (rc.canSenseLocation(l19)) m19 = rc.senseMapInfo(l19);
        l20 = myLoc.translate(-2,-2);
        if (rc.canSenseLocation(l20)) m20 = rc.senseMapInfo(l20);
        l21 = myLoc.translate(-2,-1);
        if (rc.canSenseLocation(l21)) m21 = rc.senseMapInfo(l21);
        l22 = myLoc.translate(-2,0);
        if (rc.canSenseLocation(l22)) m22 = rc.senseMapInfo(l22);
        l23 = myLoc.translate(-2,1);
        if (rc.canSenseLocation(l23)) m23 = rc.senseMapInfo(l23);
        l24 = myLoc.translate(-2,2);
        if (rc.canSenseLocation(l24)) m24 = rc.senseMapInfo(l24);
        l25 = myLoc.translate(-2,3);
        if (rc.canSenseLocation(l25)) m25 = rc.senseMapInfo(l25);
        l26 = myLoc.translate(-2,4);
        if (rc.canSenseLocation(l26)) m26 = rc.senseMapInfo(l26);
        l27 = myLoc.translate(-1,-4);
        if (rc.canSenseLocation(l27)) m27 = rc.senseMapInfo(l27);
        l28 = myLoc.translate(-1,-3);
        if (rc.canSenseLocation(l28)) m28 = rc.senseMapInfo(l28);
        l29 = myLoc.translate(-1,-2);
        if (rc.canSenseLocation(l29)) m29 = rc.senseMapInfo(l29);
        l30 = myLoc.translate(-1,-1);
        if (rc.canSenseLocation(l30)) m30 = rc.senseMapInfo(l30);
        l31 = myLoc.translate(-1,0);
        if (rc.canSenseLocation(l31)) m31 = rc.senseMapInfo(l31);
        l32 = myLoc.translate(-1,1);
        if (rc.canSenseLocation(l32)) m32 = rc.senseMapInfo(l32);
        l33 = myLoc.translate(-1,2);
        if (rc.canSenseLocation(l33)) m33 = rc.senseMapInfo(l33);
        l34 = myLoc.translate(-1,3);
        if (rc.canSenseLocation(l34)) m34 = rc.senseMapInfo(l34);
        l35 = myLoc.translate(-1,4);
        if (rc.canSenseLocation(l35)) m35 = rc.senseMapInfo(l35);
        l36 = myLoc.translate(0,-4);
        if (rc.canSenseLocation(l36)) m36 = rc.senseMapInfo(l36);
        l37 = myLoc.translate(0,-3);
        if (rc.canSenseLocation(l37)) m37 = rc.senseMapInfo(l37);
        l38 = myLoc.translate(0,-2);
        if (rc.canSenseLocation(l38)) m38 = rc.senseMapInfo(l38);
        l39 = myLoc.translate(0,-1);
        if (rc.canSenseLocation(l39)) m39 = rc.senseMapInfo(l39);
        l40 = myLoc.translate(0,0);
        if (rc.canSenseLocation(l40)) m40 = rc.senseMapInfo(l40);
        l41 = myLoc.translate(0,1);
        if (rc.canSenseLocation(l41)) m41 = rc.senseMapInfo(l41);
        l42 = myLoc.translate(0,2);
        if (rc.canSenseLocation(l42)) m42 = rc.senseMapInfo(l42);
        l43 = myLoc.translate(0,3);
        if (rc.canSenseLocation(l43)) m43 = rc.senseMapInfo(l43);
        l44 = myLoc.translate(0,4);
        if (rc.canSenseLocation(l44)) m44 = rc.senseMapInfo(l44);
        l45 = myLoc.translate(1,-4);
        if (rc.canSenseLocation(l45)) m45 = rc.senseMapInfo(l45);
        l46 = myLoc.translate(1,-3);
        if (rc.canSenseLocation(l46)) m46 = rc.senseMapInfo(l46);
        l47 = myLoc.translate(1,-2);
        if (rc.canSenseLocation(l47)) m47 = rc.senseMapInfo(l47);
        l48 = myLoc.translate(1,-1);
        if (rc.canSenseLocation(l48)) m48 = rc.senseMapInfo(l48);
        l49 = myLoc.translate(1,0);
        if (rc.canSenseLocation(l49)) m49 = rc.senseMapInfo(l49);
        l50 = myLoc.translate(1,1);
        if (rc.canSenseLocation(l50)) m50 = rc.senseMapInfo(l50);
        l51 = myLoc.translate(1,2);
        if (rc.canSenseLocation(l51)) m51 = rc.senseMapInfo(l51);
        l52 = myLoc.translate(1,3);
        if (rc.canSenseLocation(l52)) m52 = rc.senseMapInfo(l52);
        l53 = myLoc.translate(1,4);
        if (rc.canSenseLocation(l53)) m53 = rc.senseMapInfo(l53);
        l54 = myLoc.translate(2,-4);
        if (rc.canSenseLocation(l54)) m54 = rc.senseMapInfo(l54);
        l55 = myLoc.translate(2,-3);
        if (rc.canSenseLocation(l55)) m55 = rc.senseMapInfo(l55);
        l56 = myLoc.translate(2,-2);
        if (rc.canSenseLocation(l56)) m56 = rc.senseMapInfo(l56);
        l57 = myLoc.translate(2,-1);
        if (rc.canSenseLocation(l57)) m57 = rc.senseMapInfo(l57);
        l58 = myLoc.translate(2,0);
        if (rc.canSenseLocation(l58)) m58 = rc.senseMapInfo(l58);
        l59 = myLoc.translate(2,1);
        if (rc.canSenseLocation(l59)) m59 = rc.senseMapInfo(l59);
        l60 = myLoc.translate(2,2);
        if (rc.canSenseLocation(l60)) m60 = rc.senseMapInfo(l60);
        l61 = myLoc.translate(2,3);
        if (rc.canSenseLocation(l61)) m61 = rc.senseMapInfo(l61);
        l62 = myLoc.translate(2,4);
        if (rc.canSenseLocation(l62)) m62 = rc.senseMapInfo(l62);
        l64 = myLoc.translate(3,-3);
        if (rc.canSenseLocation(l64)) m64 = rc.senseMapInfo(l64);
        l65 = myLoc.translate(3,-2);
        if (rc.canSenseLocation(l65)) m65 = rc.senseMapInfo(l65);
        l66 = myLoc.translate(3,-1);
        if (rc.canSenseLocation(l66)) m66 = rc.senseMapInfo(l66);
        l67 = myLoc.translate(3,0);
        if (rc.canSenseLocation(l67)) m67 = rc.senseMapInfo(l67);
        l68 = myLoc.translate(3,1);
        if (rc.canSenseLocation(l68)) m68 = rc.senseMapInfo(l68);
        l69 = myLoc.translate(3,2);
        if (rc.canSenseLocation(l69)) m69 = rc.senseMapInfo(l69);
        l70 = myLoc.translate(3,3);
        if (rc.canSenseLocation(l70)) m70 = rc.senseMapInfo(l70);
        l74 = myLoc.translate(4,-2);
        if (rc.canSenseLocation(l74)) m74 = rc.senseMapInfo(l74);
        l75 = myLoc.translate(4,-1);
        if (rc.canSenseLocation(l75)) m75 = rc.senseMapInfo(l75);
        l76 = myLoc.translate(4,0);
        if (rc.canSenseLocation(l76)) m76 = rc.senseMapInfo(l76);
        l77 = myLoc.translate(4,1);
        if (rc.canSenseLocation(l77)) m77 = rc.senseMapInfo(l77);
        l78 = myLoc.translate(4,2);
        if (rc.canSenseLocation(l78)) m78 = rc.senseMapInfo(l78);

        int dx = myLoc.x%10, dy = myLoc.y%10;
        int dencode = dx*10 + dy;
        switch(dencode){
            case 0:
                return process0();
            case 1:
                return process1();
            case 2:
                return process2();
            case 3:
                return process3();
            case 4:
                return process4();
            case 5:
                return process5();
            case 6:
                return process6();
            case 7:
                return process7();
            case 8:
                return process8();
            case 9:
                return process9();
            case 10:
                return process10();
            case 11:
                return process11();
            case 12:
                return process12();
            case 13:
                return process13();
            case 14:
                return process14();
            case 15:
                return process15();
            case 16:
                return process16();
            case 17:
                return process17();
            case 18:
                return process18();
            case 19:
                return process19();
            case 20:
                return process20();
            case 21:
                return process21();
            case 22:
                return process22();
            case 23:
                return process23();
            case 24:
                return process24();
            case 25:
                return process25();
            case 26:
                return process26();
            case 27:
                return process27();
            case 28:
                return process28();
            case 29:
                return process29();
            case 30:
                return process30();
            case 31:
                return process31();
            case 32:
                return process32();
            case 33:
                return process33();
            case 34:
                return process34();
            case 35:
                return process35();
            case 36:
                return process36();
            case 37:
                return process37();
            case 38:
                return process38();
            case 39:
                return process39();
            case 40:
                return process40();
            case 41:
                return process41();
            case 42:
                return process42();
            case 43:
                return process43();
            case 44:
                return process44();
            case 45:
                return process45();
            case 46:
                return process46();
            case 47:
                return process47();
            case 48:
                return process48();
            case 49:
                return process49();
            case 50:
                return process50();
            case 51:
                return process51();
            case 52:
                return process52();
            case 53:
                return process53();
            case 54:
                return process54();
            case 55:
                return process55();
            case 56:
                return process56();
            case 57:
                return process57();
            case 58:
                return process58();
            case 59:
                return process59();
            case 60:
                return process60();
            case 61:
                return process61();
            case 62:
                return process62();
            case 63:
                return process63();
            case 64:
                return process64();
            case 65:
                return process65();
            case 66:
                return process66();
            case 67:
                return process67();
            case 68:
                return process68();
            case 69:
                return process69();
            case 70:
                return process70();
            case 71:
                return process71();
            case 72:
                return process72();
            case 73:
                return process73();
            case 74:
                return process74();
            case 75:
                return process75();
            case 76:
                return process76();
            case 77:
                return process77();
            case 78:
                return process78();
            case 79:
                return process79();
            case 80:
                return process80();
            case 81:
                return process81();
            case 82:
                return process82();
            case 83:
                return process83();
            case 84:
                return process84();
            case 85:
                return process85();
            case 86:
                return process86();
            case 87:
                return process87();
            case 88:
                return process88();
            case 89:
                return process89();
            case 90:
                return process90();
            case 91:
                return process91();
            case 92:
                return process92();
            case 93:
                return process93();
            case 94:
                return process94();
            case 95:
                return process95();
            case 96:
                return process96();
            case 97:
                return process97();
            case 98:
                return process98();
            case 99:
                return process99();
        }
        return null;
    }


    static MapLocation process0() throws GameActionException { // (0,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l41, true);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l51, true);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l59, true);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l60, false);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l43, false);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l67, true);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process1() throws GameActionException { // (0,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l50, true);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l58, true);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l51, false);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l60, true);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l43, false);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l67, false);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process2() throws GameActionException { // (0,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l49, true);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l51, true);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l57, true);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l59, true);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l60, false);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l67, true);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process3() throws GameActionException { // (0,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l48, true);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l50, true);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l58, true);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l56, true);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l67, false);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process4() throws GameActionException { // (0,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l49, true);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l47, true);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l57, true);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l67, false);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process5() throws GameActionException { // (0,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l48, true);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l56, true);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process6() throws GameActionException { // (0,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l47, true);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process7() throws GameActionException { // (0,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process8() throws GameActionException { // (0,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process9() throws GameActionException { // (0,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process10() throws GameActionException { // (1,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l32, true);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l50, true);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l42, true);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l58, true);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l51, false);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l60, true);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l43, false);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l67, false);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process11() throws GameActionException { // (1,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l41, true);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l49, true);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l51, true);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l57, true);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l59, true);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l60, false);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l43, true);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l67, false);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process12() throws GameActionException { // (1,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l48, true);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l50, true);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l42, true);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l58, true);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l51, false);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l56, true);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l60, false);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l67, false);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process13() throws GameActionException { // (1,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l41, true);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l49, true);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l47, true);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l57, true);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l67, true);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process14() throws GameActionException { // (1,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l48, true);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l56, true);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l67, false);
                }
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process15() throws GameActionException { // (1,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l47, true);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process16() throws GameActionException { // (1,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process17() throws GameActionException { // (1,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process18() throws GameActionException { // (1,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process19() throws GameActionException { // (1,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process20() throws GameActionException { // (2,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l41, true);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l49, true);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l23, true);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l33, true);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l51, true);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l60, false);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l43, true);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process21() throws GameActionException { // (2,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l32, true);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l48, true);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l50, true);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l42, true);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l51, false);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l60, true);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l43, false);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process22() throws GameActionException { // (2,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l41, true);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l49, true);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l33, true);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l47, true);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l51, false);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l59, true);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l60, false);
                }
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process23() throws GameActionException { // (2,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l32, true);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l48, true);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l58, true);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l59, false);
                }
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process24() throws GameActionException { // (2,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l58, false);
                }
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l47, true);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l57, true);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process25() throws GameActionException { // (2,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l57, false);
                }
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l56, true);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process26() throws GameActionException { // (2,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l56, false);
                }
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process27() throws GameActionException { // (2,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process28() throws GameActionException { // (2,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process29() throws GameActionException { // (2,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process30() throws GameActionException { // (3,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l32, true);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l42, true);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l51, false);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l24, true);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l43, false);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process31() throws GameActionException { // (3,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l41, true);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l23, true);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l33, true);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l51, true);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l43, false);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process32() throws GameActionException { // (3,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l32, true);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l50, true);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l51, false);
                }
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l24, true);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process33() throws GameActionException { // (3,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l49, true);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l50, false);
                }
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l23, true);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process34() throws GameActionException { // (3,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l49, false);
                }
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l48, true);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process35() throws GameActionException { // (3,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l48, false);
                }
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l47, true);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process36() throws GameActionException { // (3,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l47, false);
                }
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process37() throws GameActionException { // (3,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process38() throws GameActionException { // (3,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process39() throws GameActionException { // (3,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process40() throws GameActionException { // (4,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l23, true);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l33, true);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l43, true);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process41() throws GameActionException { // (4,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l32, true);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l42, true);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l24, true);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l43, false);
                }
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process42() throws GameActionException { // (4,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l41, true);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l42, false);
                }
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l23, true);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process43() throws GameActionException { // (4,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l40, true);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l41, false);
                }
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process44() throws GameActionException { // (4,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l40, false);
                }
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l39, true);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process45() throws GameActionException { // (4,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l39, false);
                }
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l38, true);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process46() throws GameActionException { // (4,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l38, false);
                }
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l37, true);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process47() throws GameActionException { // (4,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l37, false);
                }
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process48() throws GameActionException { // (4,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process49() throws GameActionException { // (4,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process50() throws GameActionException { // (5,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l24, true);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process51() throws GameActionException { // (5,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l23, true);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l33, true);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process52() throws GameActionException { // (5,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l32, true);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l33, false);
                }
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process53() throws GameActionException { // (5,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l31, true);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l32, false);
                }
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process54() throws GameActionException { // (5,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l31, false);
                }
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l30, true);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process55() throws GameActionException { // (5,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l30, false);
                }
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l29, true);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process56() throws GameActionException { // (5,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l29, false);
                }
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process57() throws GameActionException { // (5,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process58() throws GameActionException { // (5,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process59() throws GameActionException { // (5,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process60() throws GameActionException { // (6,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process61() throws GameActionException { // (6,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l24, true);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process62() throws GameActionException { // (6,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l23, true);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l24, false);
                }
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process63() throws GameActionException { // (6,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l22, true);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l23, false);
                }
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process64() throws GameActionException { // (6,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l22, false);
                }
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l21, true);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process65() throws GameActionException { // (6,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l21, false);
                }
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l20, true);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process66() throws GameActionException { // (6,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l20, false);
                }
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process67() throws GameActionException { // (6,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process68() throws GameActionException { // (6,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process69() throws GameActionException { // (6,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process70() throws GameActionException { // (7,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process71() throws GameActionException { // (7,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process72() throws GameActionException { // (7,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process73() throws GameActionException { // (7,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                if (ready){
                    rc.attack(l13, true);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process74() throws GameActionException { // (7,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                if (ready){
                    rc.attack(l13, false);
                }
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process75() throws GameActionException { // (7,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process76() throws GameActionException { // (7,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process77() throws GameActionException { // (7,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process78() throws GameActionException { // (7,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process79() throws GameActionException { // (7,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process80() throws GameActionException { // (8,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process81() throws GameActionException { // (8,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process82() throws GameActionException { // (8,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process83() throws GameActionException { // (8,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process84() throws GameActionException { // (8,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process85() throws GameActionException { // (8,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process86() throws GameActionException { // (8,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process87() throws GameActionException { // (8,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process88() throws GameActionException { // (8,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process89() throws GameActionException { // (8,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process90() throws GameActionException { // (9,0)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process91() throws GameActionException { // (9,1)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process92() throws GameActionException { // (9,2)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process93() throws GameActionException { // (9,3)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process94() throws GameActionException { // (9,4)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process95() throws GameActionException { // (9,5)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process96() throws GameActionException { // (9,6)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process97() throws GameActionException { // (9,7)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process98() throws GameActionException { // (9,8)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l78;
            }
        }
        return ans;
    }


    static MapLocation process99() throws GameActionException { // (9,9)
        MapLocation ans = null;
        if (rc.onTheMap(l40) && !m40.isWall() && !m40.hasRuin() && (maxT || !Map.isNearRuin(l40))){ // (0,0)
            PaintType p = m40.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l40;
            }
        }
        if (rc.onTheMap(l31) && !m31.isWall() && !m31.hasRuin() && (maxT || !Map.isNearRuin(l31))){ // (-1,0)
            PaintType p = m31.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l31;
            }
        }
        if (rc.onTheMap(l39) && !m39.isWall() && !m39.hasRuin() && (maxT || !Map.isNearRuin(l39))){ // (0,-1)
            PaintType p = m39.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l39;
            }
        }
        if (rc.onTheMap(l41) && !m41.isWall() && !m41.hasRuin() && (maxT || !Map.isNearRuin(l41))){ // (0,1)
            PaintType p = m41.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l41;
            }
        }
        if (rc.onTheMap(l49) && !m49.isWall() && !m49.hasRuin() && (maxT || !Map.isNearRuin(l49))){ // (1,0)
            PaintType p = m49.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l49;
            }
        }
        if (rc.onTheMap(l30) && !m30.isWall() && !m30.hasRuin() && (maxT || !Map.isNearRuin(l30))){ // (-1,-1)
            PaintType p = m30.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l30;
            }
        }
        if (rc.onTheMap(l32) && !m32.isWall() && !m32.hasRuin() && (maxT || !Map.isNearRuin(l32))){ // (-1,1)
            PaintType p = m32.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l32;
            }
        }
        if (rc.onTheMap(l48) && !m48.isWall() && !m48.hasRuin() && (maxT || !Map.isNearRuin(l48))){ // (1,-1)
            PaintType p = m48.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l48;
            }
        }
        if (rc.onTheMap(l50) && !m50.isWall() && !m50.hasRuin() && (maxT || !Map.isNearRuin(l50))){ // (1,1)
            PaintType p = m50.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l50;
            }
        }
        if (rc.onTheMap(l22) && !m22.isWall() && !m22.hasRuin() && (maxT || !Map.isNearRuin(l22))){ // (-2,0)
            PaintType p = m22.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l22;
            }
        }
        if (rc.onTheMap(l38) && !m38.isWall() && !m38.hasRuin() && (maxT || !Map.isNearRuin(l38))){ // (0,-2)
            PaintType p = m38.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l38;
            }
        }
        if (rc.onTheMap(l42) && !m42.isWall() && !m42.hasRuin() && (maxT || !Map.isNearRuin(l42))){ // (0,2)
            PaintType p = m42.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l42;
            }
        }
        if (rc.onTheMap(l58) && !m58.isWall() && !m58.hasRuin() && (maxT || !Map.isNearRuin(l58))){ // (2,0)
            PaintType p = m58.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l58;
            }
        }
        if (rc.onTheMap(l21) && !m21.isWall() && !m21.hasRuin() && (maxT || !Map.isNearRuin(l21))){ // (-2,-1)
            PaintType p = m21.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l21;
            }
        }
        if (rc.onTheMap(l23) && !m23.isWall() && !m23.hasRuin() && (maxT || !Map.isNearRuin(l23))){ // (-2,1)
            PaintType p = m23.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l23;
            }
        }
        if (rc.onTheMap(l29) && !m29.isWall() && !m29.hasRuin() && (maxT || !Map.isNearRuin(l29))){ // (-1,-2)
            PaintType p = m29.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l29;
            }
        }
        if (rc.onTheMap(l33) && !m33.isWall() && !m33.hasRuin() && (maxT || !Map.isNearRuin(l33))){ // (-1,2)
            PaintType p = m33.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l33;
            }
        }
        if (rc.onTheMap(l47) && !m47.isWall() && !m47.hasRuin() && (maxT || !Map.isNearRuin(l47))){ // (1,-2)
            PaintType p = m47.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l47;
            }
        }
        if (rc.onTheMap(l51) && !m51.isWall() && !m51.hasRuin() && (maxT || !Map.isNearRuin(l51))){ // (1,2)
            PaintType p = m51.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l51;
            }
        }
        if (rc.onTheMap(l57) && !m57.isWall() && !m57.hasRuin() && (maxT || !Map.isNearRuin(l57))){ // (2,-1)
            PaintType p = m57.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l57;
            }
        }
        if (rc.onTheMap(l59) && !m59.isWall() && !m59.hasRuin() && (maxT || !Map.isNearRuin(l59))){ // (2,1)
            PaintType p = m59.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l59;
            }
        }
        if (rc.onTheMap(l20) && !m20.isWall() && !m20.hasRuin() && (maxT || !Map.isNearRuin(l20))){ // (-2,-2)
            PaintType p = m20.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l20;
            }
        }
        if (rc.onTheMap(l24) && !m24.isWall() && !m24.hasRuin() && (maxT || !Map.isNearRuin(l24))){ // (-2,2)
            PaintType p = m24.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l24;
            }
        }
        if (rc.onTheMap(l56) && !m56.isWall() && !m56.hasRuin() && (maxT || !Map.isNearRuin(l56))){ // (2,-2)
            PaintType p = m56.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l56;
            }
        }
        if (rc.onTheMap(l60) && !m60.isWall() && !m60.hasRuin() && (maxT || !Map.isNearRuin(l60))){ // (2,2)
            PaintType p = m60.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l60;
            }
        }
        if (rc.onTheMap(l13) && !m13.isWall() && !m13.hasRuin() && (maxT || !Map.isNearRuin(l13))){ // (-3,0)
            PaintType p = m13.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l13;
            }
        }
        if (rc.onTheMap(l37) && !m37.isWall() && !m37.hasRuin() && (maxT || !Map.isNearRuin(l37))){ // (0,-3)
            PaintType p = m37.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l37;
            }
        }
        if (rc.onTheMap(l43) && !m43.isWall() && !m43.hasRuin() && (maxT || !Map.isNearRuin(l43))){ // (0,3)
            PaintType p = m43.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l43;
            }
        }
        if (rc.onTheMap(l67) && !m67.isWall() && !m67.hasRuin() && (maxT || !Map.isNearRuin(l67))){ // (3,0)
            PaintType p = m67.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l67;
            }
        }
        if (rc.onTheMap(l12) && !m12.isWall() && !m12.hasRuin() && (maxT || !Map.isNearRuin(l12))){ // (-3,-1)
            PaintType p = m12.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l12;
            }
        }
        if (rc.onTheMap(l14) && !m14.isWall() && !m14.hasRuin() && (maxT || !Map.isNearRuin(l14))){ // (-3,1)
            PaintType p = m14.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l14;
            }
        }
        if (rc.onTheMap(l28) && !m28.isWall() && !m28.hasRuin() && (maxT || !Map.isNearRuin(l28))){ // (-1,-3)
            PaintType p = m28.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l28;
            }
        }
        if (rc.onTheMap(l34) && !m34.isWall() && !m34.hasRuin() && (maxT || !Map.isNearRuin(l34))){ // (-1,3)
            PaintType p = m34.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l34;
            }
        }
        if (rc.onTheMap(l46) && !m46.isWall() && !m46.hasRuin() && (maxT || !Map.isNearRuin(l46))){ // (1,-3)
            PaintType p = m46.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l46;
            }
        }
        if (rc.onTheMap(l52) && !m52.isWall() && !m52.hasRuin() && (maxT || !Map.isNearRuin(l52))){ // (1,3)
            PaintType p = m52.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l52;
            }
        }
        if (rc.onTheMap(l66) && !m66.isWall() && !m66.hasRuin() && (maxT || !Map.isNearRuin(l66))){ // (3,-1)
            PaintType p = m66.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l66;
            }
        }
        if (rc.onTheMap(l68) && !m68.isWall() && !m68.hasRuin() && (maxT || !Map.isNearRuin(l68))){ // (3,1)
            PaintType p = m68.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l68;
            }
        }
        if (rc.onTheMap(l11) && !m11.isWall() && !m11.hasRuin() && (maxT || !Map.isNearRuin(l11))){ // (-3,-2)
            PaintType p = m11.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l11;
            }
        }
        if (rc.onTheMap(l15) && !m15.isWall() && !m15.hasRuin() && (maxT || !Map.isNearRuin(l15))){ // (-3,2)
            PaintType p = m15.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l15;
            }
        }
        if (rc.onTheMap(l19) && !m19.isWall() && !m19.hasRuin() && (maxT || !Map.isNearRuin(l19))){ // (-2,-3)
            PaintType p = m19.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l19;
            }
        }
        if (rc.onTheMap(l25) && !m25.isWall() && !m25.hasRuin() && (maxT || !Map.isNearRuin(l25))){ // (-2,3)
            PaintType p = m25.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l25;
            }
        }
        if (rc.onTheMap(l55) && !m55.isWall() && !m55.hasRuin() && (maxT || !Map.isNearRuin(l55))){ // (2,-3)
            PaintType p = m55.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l55;
            }
        }
        if (rc.onTheMap(l61) && !m61.isWall() && !m61.hasRuin() && (maxT || !Map.isNearRuin(l61))){ // (2,3)
            PaintType p = m61.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l61;
            }
        }
        if (rc.onTheMap(l65) && !m65.isWall() && !m65.hasRuin() && (maxT || !Map.isNearRuin(l65))){ // (3,-2)
            PaintType p = m65.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l65;
            }
        }
        if (rc.onTheMap(l69) && !m69.isWall() && !m69.hasRuin() && (maxT || !Map.isNearRuin(l69))){ // (3,2)
            PaintType p = m69.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l69;
            }
        }
        if (rc.onTheMap(l4) && !m4.isWall() && !m4.hasRuin() && (maxT || !Map.isNearRuin(l4))){ // (-4,0)
            PaintType p = m4.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l4;
            }
        }
        if (rc.onTheMap(l36) && !m36.isWall() && !m36.hasRuin() && (maxT || !Map.isNearRuin(l36))){ // (0,-4)
            PaintType p = m36.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l36;
            }
        }
        if (rc.onTheMap(l44) && !m44.isWall() && !m44.hasRuin() && (maxT || !Map.isNearRuin(l44))){ // (0,4)
            PaintType p = m44.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l44;
            }
        }
        if (rc.onTheMap(l76) && !m76.isWall() && !m76.hasRuin() && (maxT || !Map.isNearRuin(l76))){ // (4,0)
            PaintType p = m76.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l76;
            }
        }
        if (rc.onTheMap(l3) && !m3.isWall() && !m3.hasRuin() && (maxT || !Map.isNearRuin(l3))){ // (-4,-1)
            PaintType p = m3.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l3;
            }
        }
        if (rc.onTheMap(l5) && !m5.isWall() && !m5.hasRuin() && (maxT || !Map.isNearRuin(l5))){ // (-4,1)
            PaintType p = m5.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l5;
            }
        }
        if (rc.onTheMap(l27) && !m27.isWall() && !m27.hasRuin() && (maxT || !Map.isNearRuin(l27))){ // (-1,-4)
            PaintType p = m27.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l27;
            }
        }
        if (rc.onTheMap(l35) && !m35.isWall() && !m35.hasRuin() && (maxT || !Map.isNearRuin(l35))){ // (-1,4)
            PaintType p = m35.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l35;
            }
        }
        if (rc.onTheMap(l45) && !m45.isWall() && !m45.hasRuin() && (maxT || !Map.isNearRuin(l45))){ // (1,-4)
            PaintType p = m45.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l45;
            }
        }
        if (rc.onTheMap(l53) && !m53.isWall() && !m53.hasRuin() && (maxT || !Map.isNearRuin(l53))){ // (1,4)
            PaintType p = m53.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l53;
            }
        }
        if (rc.onTheMap(l75) && !m75.isWall() && !m75.hasRuin() && (maxT || !Map.isNearRuin(l75))){ // (4,-1)
            PaintType p = m75.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l75;
            }
        }
        if (rc.onTheMap(l77) && !m77.isWall() && !m77.hasRuin() && (maxT || !Map.isNearRuin(l77))){ // (4,1)
            PaintType p = m77.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_SECONDARY){
                return l77;
            }
        }
        if (rc.onTheMap(l10) && !m10.isWall() && !m10.hasRuin() && (maxT || !Map.isNearRuin(l10))){ // (-3,-3)
            PaintType p = m10.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l10;
            }
        }
        if (rc.onTheMap(l16) && !m16.isWall() && !m16.hasRuin() && (maxT || !Map.isNearRuin(l16))){ // (-3,3)
            PaintType p = m16.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l16;
            }
        }
        if (rc.onTheMap(l64) && !m64.isWall() && !m64.hasRuin() && (maxT || !Map.isNearRuin(l64))){ // (3,-3)
            PaintType p = m64.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l64;
            }
        }
        if (rc.onTheMap(l70) && !m70.isWall() && !m70.hasRuin() && (maxT || !Map.isNearRuin(l70))){ // (3,3)
            PaintType p = m70.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l70;
            }
        }
        if (rc.onTheMap(l2) && !m2.isWall() && !m2.hasRuin() && (maxT || !Map.isNearRuin(l2))){ // (-4,-2)
            PaintType p = m2.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l2;
            }
        }
        if (rc.onTheMap(l6) && !m6.isWall() && !m6.hasRuin() && (maxT || !Map.isNearRuin(l6))){ // (-4,2)
            PaintType p = m6.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l6;
            }
        }
        if (rc.onTheMap(l18) && !m18.isWall() && !m18.hasRuin() && (maxT || !Map.isNearRuin(l18))){ // (-2,-4)
            PaintType p = m18.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l18;
            }
        }
        if (rc.onTheMap(l26) && !m26.isWall() && !m26.hasRuin() && (maxT || !Map.isNearRuin(l26))){ // (-2,4)
            PaintType p = m26.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l26;
            }
        }
        if (rc.onTheMap(l54) && !m54.isWall() && !m54.hasRuin() && (maxT || !Map.isNearRuin(l54))){ // (2,-4)
            PaintType p = m54.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l54;
            }
        }
        if (rc.onTheMap(l62) && !m62.isWall() && !m62.hasRuin() && (maxT || !Map.isNearRuin(l62))){ // (2,4)
            PaintType p = m62.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l62;
            }
        }
        if (rc.onTheMap(l74) && !m74.isWall() && !m74.hasRuin() && (maxT || !Map.isNearRuin(l74))){ // (4,-2)
            PaintType p = m74.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l74;
            }
        }
        if (rc.onTheMap(l78) && !m78.isWall() && !m78.hasRuin() && (maxT || !Map.isNearRuin(l78))){ // (4,2)
            PaintType p = m78.getPaint();
            if (!p.isEnemy() && p != PaintType.ALLY_PRIMARY){
                return l78;
            }
        }
        return ans;
    }


}